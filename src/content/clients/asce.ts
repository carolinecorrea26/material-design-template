import type { DeepPartial } from "../types-util";
import type { SiteContent } from "../types";

/** ASCE uses the global/default content. */
export const asceContentOverrides: DeepPartial<SiteContent> = {};
