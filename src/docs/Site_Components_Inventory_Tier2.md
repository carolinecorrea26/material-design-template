# Site Components Inventory — Tier 2: Forms

> Extends: _Site Components Inventory — Tier 1: Layout & UI_
> Scope: `src/components/forms/`
> Status: Draft — items marked ⚠️ are open questions

---

## Component IDs

Form components use the prefix `FRM-###`.

---

## Forms Components

| Component ID | Component / Pattern                                              | Category           | Purpose                                                                                                                                                                                                                                                                                                                                                              | Pages / Locations                                                                                                                                                                                 | Behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Variants / States                                                                                                                                                                                   | Content / Data Inputs                                                                                                                                                                                                                                                                        | Rules / Conditions                                                                                                                                                                                                                                                                                                                                                              | Validation / Errors                                                                                                                                                       | Client Configurability                                                                                                                                                                                                                             | Accessibility                                                                                                                                                                                                                        | Source Reference                                                                                                                                                                                                                                                                                                                                                                           | Status / Open Questions                                                                                                                                                                                                                                                                            |
| ------------ | ---------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FRM-001      | `FieldRenderer`                                                  | Forms              | Central switch that renders the correct MUI input control for any `FieldDefinition`. Eliminates per-page field-type logic. All field rendering flows through this component.                                                                                                                                                                                         | All form pages via `RoutePage` and `CoverageQuestions`                                                                                                                                            | Accepts a `FieldDefinition` and a React Hook Form `control` + `errors` object. Renders one of nine input patterns (see Field Types below). Handles formatting, masking, validation rules, and optional completion/error adornments (enabled via `?inputChecks` URL flag). Two label modes are supported: `floating` (MUI `InputLabel` inside the input border) and `standard` (external `FormLabel` above the input); the active mode is controlled by a `?labelStandard` URL flag or the field's own `labelVariant` property. | One variant per input type — see FRM-001a through FRM-001i below                                                                                                                                    | `field: FieldDefinition`; `control: Control`; `errors: FieldErrors`; `hideLabel?: boolean`; `margin?: 'none' \| 'dense' \| 'normal'`; `onValueChange?: () => void`                                                                                                                           | SSN fields are masked with bullets; last typed digit is briefly revealed for 800ms then hidden. Phone fields render an inline type selector (Mobile / Home / Business) unless `showPhoneTypeSelector: false`. Currency fields auto-format with `$` and locale commas. Date fields enforce `MM/DD/YYYY` input pattern and store in `YYYY-MM-DD` format.                          | Per-field validation is computed by `getValidationRules` based on `inputType` and `format`. Errors display below the field as `FormHelperText`. Required fields show `*`. | Field `label`, `placeholder`, `helperText`, `options`, `required`, `disabled`, and `tooltip` are all sourced from `FieldDefinition` — config-driven, never hardcoded in this component. All overrides are applied via `ClientPageFieldConfig`.     | Every input has an associated label (floating `InputLabel` or external `FormLabel`). Errors linked via `FormHelperText` (MUI handles `aria-describedby`). Required fields use MUI's built-in `required` prop (adds `aria-required`). | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                                                                                                                                                                                   | The `?inputChecks` and `?labelStandard` URL flags are prototype/dev tools — ⚠️ confirm whether `labelVariant: 'standard'` will be the production mechanism for label mode, replacing the URL flag.                                                                                                 |
| FRM-001a     | `FieldRenderer` → `text` input                                   | Forms / Field Type | Plain text input. Also handles `email` (`type="email"`) and `tel` (`type="tel"`) subtypes detected via `field.format`.                                                                                                                                                                                                                                               | Profile (name fields), Contact (address, email), Membership (member ID fields), Health (physician name), and many others                                                                          | MUI `TextField`. Format-specific behavior: `email` validates against RFC-simple regex; `tel` triggers phone formatting branch (see FRM-001e); plain `text` passes through as-is.                                                                                                                                                                                                                                                                                                                                               | Default; disabled                                                                                                                                                                                   | `field.format?: 'email'` triggers email validation. No `format` = plain text.                                                                                                                                                                                                                | Email: validated on blur with regex. Plain text: no format validation, only required check.                                                                                                                                                                                                                                                                                     | Label and placeholder from `FieldDefinition`                                                                                                                              | `type="email"` renders the correct mobile keyboard; `autoComplete` from `FieldDefinition`                                                                                                                                                          | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | —                                                                                                                                                                                                                                                                                                                                                                                          |
| FRM-001b     | `FieldRenderer` → `number` input                                 | Forms / Field Type | Numeric-only input. Strips non-digit characters on change.                                                                                                                                                                                                                                                                                                           | Health (height feet/inches, weight), Profile (years self-employed, employees), others                                                                                                             | MUI `TextField` with `inputMode="numeric"` and digit sanitization on every change event.                                                                                                                                                                                                                                                                                                                                                                                                                                       | Default; disabled                                                                                                                                                                                   | `field.inputMode` defaults to `"numeric"`                                                                                                                                                                                                                                                    | Validates that value contains only digits (`/^\d+$/`). Required check applies if `field.required`.                                                                                                                                                                                                                                                                              | —                                                                                                                                                                         | `inputMode="numeric"` triggers numeric keyboard on mobile                                                                                                                                                                                          | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | —                                                                                                                                                                                                                                                                                                                                                                                          |
| FRM-001c     | `FieldRenderer` → `date` input                                   | Forms / Field Type | Date input with `MM/DD/YYYY` display format. Stores value as `YYYY-MM-DD`.                                                                                                                                                                                                                                                                                           | Membership (birth dates), Profile (self-employed dates), Health, and any date field                                                                                                               | MUI `TextField` with `inputMode="numeric"`. Formats display value character-by-character as the user types. Auto-converts to storage format when 8 digits are complete. Helper text shows `MM/DD/YYYY` as a format hint when no error is present.                                                                                                                                                                                                                                                                              | Default; disabled                                                                                                                                                                                   | `field.autoComplete` (e.g. `"bday"`)                                                                                                                                                                                                                                                         | Validates completed date format on blur via regex. Required check applies if `field.required`.                                                                                                                                                                                                                                                                                  | Helper text = `MM/DD/YYYY` by default; overridable via `field.helperText`                                                                                                 | `inputMode="numeric"` on mobile; format hint in helper text                                                                                                                                                                                        | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | —                                                                                                                                                                                                                                                                                                                                                                                          |
| FRM-001d     | `FieldRenderer` → `dropdown` / `searchable-select`               | Forms / Field Type | Dropdown select for a list of options. `dropdown` = MUI `Select`. `searchable-select` = MUI `Autocomplete` with type-ahead filtering.                                                                                                                                                                                                                                | State fields, business type, AMA physician type, AVMA graduation year, and any field with a defined `options` array                                                                               | `dropdown`: floating label uses `InputLabel`+`Select`; standard label uses external `FormLabel`+`Select displayEmpty`. `searchable-select`: always uses MUI `Autocomplete` with a `TextField` render input; stores the selected option's `value` string. Both display an empty/placeholder `MenuItem` or placeholder text when no value is selected.                                                                                                                                                                           | Default; disabled; `searchable-select` variant                                                                                                                                                      | `field.options: FieldOption[]`; `field.placeholder` (shown as empty item label, defaults to `"Select"`)                                                                                                                                                                                      | Required check only (no format validation).                                                                                                                                                                                                                                                                                                                                     | `field.options` are config-driven; labels are client-configurable via field config overrides                                                                              | Floating label linked via `labelId` + `InputLabel`; standard label via `FormLabel`. Options list is keyboard-navigable.                                                                                                                            | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | —                                                                                                                                                                                                                                                                                                                                                                                          |
| FRM-001e     | `FieldRenderer` → `phone` format                                 | Forms / Field Type | Phone number input with auto-formatting to `(###) ###-####` and an inline phone-type selector (Mobile / Home / Business) embedded as an end adornment.                                                                                                                                                                                                               | Contact (phone), Profile (business phone, spouse phone), Beneficiary, Health (physician phone), and any field with `format: 'phone'`                                                              | Formats digits as the user types. By default, renders an inline MUI `Select` (variant `standard`, no underline) for phone type in the end adornment, bound to a companion field (default: `phone-type`; overridable via `field.phoneTypeFieldId`). Set `field.showPhoneTypeSelector: false` to suppress the type selector and render as a plain formatted text input.                                                                                                                                                          | With type selector (default); without type selector                                                                                                                                                 | `field.phoneTypeFieldId?: string` (companion field name, default `"phone-type"`); `field.showPhoneTypeSelector?: boolean` (default `true`)                                                                                                                                                   | Validates exactly 10 digits after stripping formatting. Required check applies if `field.required`.                                                                                                                                                                                                                                                                             | Phone type options (Mobile/Home/Business) are currently hardcoded — ⚠️ confirm if these need to be client-configurable                                                    | `type="tel"` for correct mobile keyboard; formatted value is human-readable                                                                                                                                                                        | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | ⚠️ Phone type options are hardcoded. Confirm if client config needs to override them.                                                                                                                                                                                                                                                                                                      |
| FRM-001f     | `FieldRenderer` → `ssn` format                                   | Forms / Field Type | SSN input with masking. Digits are replaced with bullet characters (•) as the user types, with the most recently typed digit shown briefly (800ms) before being masked. Stores the formatted value with spaces (`### ## ####`).                                                                                                                                      | Profile (SSN for applicant and spouse)                                                                                                                                                            | Custom `SsnField` sub-component wraps `renderTextLikeField`. Tracks real (unmasked) value in RHF state; displays a masked version computed on every render. Handles both addition (append new digit) and deletion (trim last digit) by comparing bullet count in display vs input. `autoComplete: "off"` is always set.                                                                                                                                                                                                        | Default masking; last-digit reveal (transient, 800ms)                                                                                                                                               | None beyond `field.id` (SSN fields are identified by `format: 'ssn'`)                                                                                                                                                                                                                        | Validates exactly 9 digits after stripping formatting. Required check applies if `field.required`.                                                                                                                                                                                                                                                                              | —                                                                                                                                                                         | `autoComplete="off"` prevents browser autofill. Masking protects sensitive data visually. ⚠️ Screen reader behavior for masked input should be verified — bullets may be read literally.                                                           | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | ⚠️ Confirm screen reader behavior for the masked SSN input. Consider adding `aria-label` override to announce it as "Social Security Number" regardless of display value.                                                                                                                                                                                                                  |
| FRM-001g     | `FieldRenderer` → `currency` format                              | Forms / Field Type | Currency input that auto-formats to `$1,000` notation on every keystroke. Strips non-digit characters and prepends `$`. Max 12 digits.                                                                                                                                                                                                                               | Profile (average monthly income, monthly business expenses, disability benefit amounts, net worth, etc.)                                                                                          | Detected by `field.format === 'currency'` OR by membership in a hardcoded `CURRENCY_FIELD_IDS` set (legacy). `inputMode="numeric"` for mobile keyboard.                                                                                                                                                                                                                                                                                                                                                                        | Default; disabled                                                                                                                                                                                   | `field.format?: 'currency'` or field ID in `CURRENCY_FIELD_IDS`                                                                                                                                                                                                                              | No format-specific validation beyond required check. Stored value includes `$` and commas (formatted string).                                                                                                                                                                                                                                                                   | `CURRENCY_FIELD_IDS` is a hardcoded set — ⚠️ confirm if all currency fields now use `field.format: 'currency'` and the ID set should be removed                           | `inputMode="numeric"` on mobile                                                                                                                                                                                                                    | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | ⚠️ `CURRENCY_FIELD_IDS` hardcoded set is a legacy mechanism. Audit whether all currency fields now declare `format: 'currency'` and remove the set if so.                                                                                                                                                                                                                                  |
| FRM-001h     | `FieldRenderer` → `radio` input                                  | Forms / Field Type | Radio button group rendered as a MUI `ToggleButtonGroup` where each option is a full-width `ToggleButton` containing a `Radio` indicator and label. The entire button row is the tap target.                                                                                                                                                                         | Yes/No questions throughout Health and Profile (smoker, replacing insurance, has disability, etc.); any field with `inputType: 'radio'` and `field.options`                                       | `ToggleButtonGroup exclusive` bound to the field value. Each option shows the `Radio` control (pointer-events disabled — the parent button handles interaction) plus the label text. Null selections are prevented (once selected, a radio group cannot be de-selected).                                                                                                                                                                                                                                                       | Unselected (no value); option selected; disabled                                                                                                                                                    | `field.options: FieldOption[]`                                                                                                                                                                                                                                                               | Required check only.                                                                                                                                                                                                                                                                                                                                                            | Option labels are config-driven via `field.options`                                                                                                                       | `ToggleButton` has implicit button role; `Radio` inside is decorative (`pointerEvents: none`). ⚠️ Verify that the `ToggleButtonGroup` announces selection changes correctly to screen readers — `aria-pressed` vs `aria-checked` semantics differ. | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | ⚠️ Confirm screen reader semantics: `ToggleButtonGroup` uses `aria-pressed`, but radio group semantics (`aria-checked`, `role="radio"`) may be more appropriate here.                                                                                                                                                                                                                      |
| FRM-001i     | `FieldRenderer` → `checkbox` / `checkbox-group` / `multi-select` | Forms / Field Type | Three checkbox patterns for different use cases. `checkbox`: single boolean toggle rendered as a `SelectableOptionRow` (full-width labeled row). `checkbox-group`: multiple independent checkboxes each in a `SelectableOptionRow`, storing an array of selected values. `multi-select`: MUI `Select multiple` with checkboxes inside `MenuItem`s, storing an array. | `checkbox`: consent/authorization fields (Review, E-Sign, bank authorization). `checkbox-group`: tobacco products (Health). `multi-select`: tobacco products (alternate), multi-option selections | `checkbox` stores `boolean`. `checkbox-group` and `multi-select` both store `string[]`. All three validate that at least one item is selected when `field.required`. `checkbox-group` renders options as visible rows; `multi-select` hides them behind a dropdown.                                                                                                                                                                                                                                                            | Unchecked; checked; error (required, not selected)                                                                                                                                                  | `field.options: FieldOption[]` for group/multi; no options for single checkbox                                                                                                                                                                                                               | Required: `checkbox` must be `true`; `checkbox-group` and `multi-select` must have `value.length > 0`.                                                                                                                                                                                                                                                                          | Option labels are config-driven                                                                                                                                           | `checkbox`: the `SelectableOptionRow` label acts as the click target; input is inside. `checkbox-group`: same pattern per option. `multi-select`: standard MUI Select keyboard behavior.                                                           | `src/components/forms/FieldRenderer.tsx`                                                                                                                                                                                             | ⚠️ `percent` format also exists in `FieldRenderer` (strips to max 3 digits, no `$`) but has no corresponding `FieldInputType` — it's detected via `field.format` only. Confirm whether `percent` should be added to `FieldInputType` for clarity.                                                                                                                                          |
| FRM-002      | `SelectableOptionRow` (named `OptionRow` in inventory)           | Forms              | Full-width labeled row used as the visual shell for `checkbox`, `checkbox-group`, and `radio`-style inputs. Renders as a `<label>` element so the entire row is a click/tap target.                                                                                                                                                                                  | Used internally by `FieldRenderer` for `checkbox`, `checkbox-group`, and `radio` fields. Also used directly in `Coverage` page (ProductCatalog) for plan selection rows.                          | Renders a bordered, rounded `<Box component="label">` with flex layout. Children slot in the input control (checkbox/radio), label text, and optional trailing icon. Hover and selected states are driven by the child input's checked state, not by this component directly.                                                                                                                                                                                                                                                  | Default (unselected); hover; ⚠️ selected visual state (currently relies on parent/child styling — confirm if `SelectableOptionRow` should accept a `selected` prop to apply its own selected style) | `children: ReactNode`; `htmlFor?: string`                                                                                                                                                                                                                                                    | Always used as a wrapper — no standalone validation                                                                                                                                                                                                                                                                                                                             | None — purely structural                                                                                                                                                  | No interactive role on the Box itself (it's a `<label>`); the child input owns the accessible name and state                                                                                                                                       | `src/components/forms/OptionRow.tsx`                                                                                                                                                                                                 | ⚠️ Confirm whether `SelectableOptionRow` should accept a `selected` prop to apply a visual selected state (highlighted border/background), or whether selected styling is always inherited from the child input's checked state via CSS.                                                                                                                                                   |
| FRM-003      | `SubQuestionContainer` (named `ConditionalGroup` in file)        | Forms              | Indented container with a blue left border, used to visually group follow-up questions that are conditional on a prior answer.                                                                                                                                                                                                                                       | Health page (condition follow-ups); Coverage questions (`CoverageQuestions`) wherever a section has `visibleWhen` rules beyond structural gates                                                   | Renders a `Box` with `ml: 1`, `px: 2`, and a `4px solid primary.main` left border. Pure layout — no show/hide logic. The parent (`RoutePage` or `CoverageQuestions`) decides when to render it.                                                                                                                                                                                                                                                                                                                                | Default only — no variants                                                                                                                                                                          | `children: ReactNode`                                                                                                                                                                                                                                                                        | Rendered by parent when a section is determined to be "conditional"                                                                                                                                                                                                                                                                                                             | None                                                                                                                                                                      | No interactive role; decorative grouping container                                                                                                                                                                                                 | `src/components/forms/ConditionalGroup.tsx`                                                                                                                                                                                          | ⚠️ The file is named `ConditionalGroup.tsx` but exports `SubQuestionContainer`. These should be aligned. Recommend renaming the export to `ConditionalGroup` (or the file to `SubQuestionContainer.tsx`) to eliminate confusion. Also confirm: should this component handle its own show/hide (becoming the `ConditionalFieldGroup` described in Tier 1), or remain a pure visual wrapper? |
| FRM-004      | `DynamicList` (named `AddList` in source)                        | Forms              | Renders a list of user-entered items with Add, Edit, and Remove controls. Each item is entered via a Dialog form (MUI `Dialog`) containing `FieldRenderer` fields, then displayed as a summary row (`DynamicListItem`).                                                                                                                                              | Beneficiary page (multiple beneficiaries). ⚠️ Confirm any other pages.                                                                                                                            | Items are managed via React Hook Form's `useFieldArray`. The Add button opens a Dialog with a mini-form (own `useForm` instance). On Save, item data is mapped from field IDs to storage keys via `fieldToKey`. Edit opens the same Dialog pre-populated. Remove deletes immediately — no confirmation dialog. Supports an optional `gridFields` list to render certain fields side-by-side in a 2-column grid.                                                                                                                | Empty (no items, only Add button); populated (items + Add button); ⚠️ no max-count enforcement currently visible                                                                                    | `control`; `name: string` (field array name); `label: string` (used in button and dialog title); `mapping.fields: FieldDefinition[]`; `mapping.fieldToKey: Record<fieldId, itemKey>`; `mapping.gridFields?: string[]` (field IDs to render in 2-col grid); `renderItem: (item) => ReactNode` | No min/max count enforcement currently implemented — ⚠️ confirm whether min (at least 1 beneficiary) and max counts are needed                                                                                                                                                                                                                                                  | None within the component; item-level field validation uses standard `FieldRenderer` rules inside the Dialog form                                                         | `label` string is client-configurable (passed in by the page); field definitions are config-driven                                                                                                                                                 | Add/Edit/Remove buttons are labeled. Dialog has a title. ⚠️ Dialog focus management on open/close should be verified — MUI Dialog handles this but confirm `initialFocus` behavior.                                                  | `src/components/forms/DynamicList.tsx`                                                                                                                                                                                                                                                                                                                                                     | ⚠️ Source name is `AddList` — should be renamed to `DynamicList` to match the inventory and avoid confusion with simple "add" patterns. ⚠️ No min/max item enforcement — confirm requirements (at least 1 beneficiary required?). ⚠️ Remove has no confirmation dialog — confirm if one is needed. |
| FRM-005      | `DynamicListItem` (named `AddListItem` in source)                | Forms              | Summary row for a single item in a `DynamicList`. Displays item content with Edit and Remove action buttons.                                                                                                                                                                                                                                                         | Used internally by `DynamicList`                                                                                                                                                                  | Renders item content in a flex row with Edit (text button) and Remove (red text button) on the right. Removal is immediate with no confirmation.                                                                                                                                                                                                                                                                                                                                                                               | Default                                                                                                                                                                                             | `children: ReactNode`; `onEdit: () => void`; `onRemove: () => void`                                                                                                                                                                                                                          | Always rendered as a child of `DynamicList` for each item in the field array                                                                                                                                                                                                                                                                                                    | None                                                                                                                                                                      | Remove is destructive with no undo — ⚠️ confirm if a confirmation step is needed                                                                                                                                                                   | Edit and Remove buttons are labeled. ⚠️ Confirm that Remove button's accessible name makes clear which item will be removed (e.g. "Remove Beneficiary: John Smith" rather than just "Remove").                                       | `src/components/forms/DynamicListItem.tsx`                                                                                                                                                                                                                                                                                                                                                 | ⚠️ Source name is `AddListItem` — should be renamed to `DynamicListItem`. ⚠️ Remove button accessible name may be too generic when multiple items exist.                                                                                                                                           |
| FRM-006      | `ApplicantSection`                                               | Forms / Layout     | Wrapper that scopes a group of fields to a single applicant, rendering an applicant-variant `SectionTitle` banner above the fields. Handles the single-applicant case (no label) and multi-applicant case (with label).                                                                                                                                              | Beneficiary, Contact, Profile, Health, Coverage (`CoverageQuestions`)                                                                                                                             | When `showLabel=true` (default): renders a `Box` with `mb: 4, mt: 3` containing a `SectionTitle variant="applicant"` banner followed by an optional info `Alert` and then the children. When `showLabel=false`: renders children directly (with optional note), no banner. Applicant label text comes from `applicantSectionTitles` config keyed by `ApplicantSectionId`.                                                                                                                                                      | With label (default); without label (`showLabel=false`)                                                                                                                                             | `applicant: ApplicantSectionId` (`'self' \| 'spouse' \| 'child-1' \| 'child-2'`…); `children`; `showLabel?: boolean`; `icon?: SvgIconComponent`; `note?: string`                                                                                                                             | Rendered once per covered applicant; count and which applicants appear is determined by coverage selections upstream                                                                                                                                                                                                                                                            | None at this level — field validation owned by children                                                                                                                   | Applicant label strings are sourced from `applicantSectionTitles` config — client-configurable there                                                                                                                                               | `SectionTitle variant="applicant"` renders as a heading (see LAY-005 for heading level). Info `Alert` has `role="status"` from MUI.                                                                                                  | `src/components/forms/ApplicantSection.tsx`                                                                                                                                                                                                                                                                                                                                                | ⚠️ Confirm the full set of valid `ApplicantSectionId` values and whether they are typed in `formSectionTitle.ts` or need to be.                                                                                                                                                                    |
| FRM-007      | `CoverageQuestions`                                              | Forms              | Renders the per-applicant coverage question sections on the Coverage page. Handles section visibility (which categories are selected, which applicants are covered) and conditional sub-question grouping.                                                                                                                                                           | Coverage page only                                                                                                                                                                                | Filters page sections into self/spouse buckets. Evaluates section visibility against both category-selection rules and `visibleWhen` field rules. Wraps conditional sections in `SubQuestionContainer` (FRM-003). When there is a spouse, wraps each applicant's sections in `ApplicantSection`; otherwise renders self sections flat. Returns `null` if no categories are selected or no sections are visible.                                                                                                                | With spouse (applicant-wrapped sections); without spouse (flat sections); no visible sections (returns null)                                                                                        | `control`; `errors`; `watchedValues`; `allFields`; `pageSections`; `selectedCategories: CoverageCategoryId[]`; `categoryNeedsGender/Smoker/Di/Oo/Hours: boolean`; `hasSpouse: boolean`; `onFieldChange?`; `coverageQuestions?: ClientCoverageQuestions`                                      | Section visibility is a two-pass check: (1) `isCategorySectionVisible` — checks `coverageQuestions` client config or falls back to default category-need flags; (2) `isSectionRulesVisible` — checks `section.visibleWhen` field value rules. The `visibleWhen` check on the coverage page intentionally skips the applicant coverage gate (fields haven't been submitted yet). | Field-level validation owned by `FieldRenderer` children                                                                                                                  | `coverageQuestions` client config maps categories to section IDs, fully overriding default behavior. Default category-section mapping is hardcoded in `defaultPersonalSections`, `defaultWorkIncomeSections`, `defaultBusinessSections`.           | Section-level `aria` is handled by `ApplicantSection` and `SectionTitle` children. No additional landmark roles needed here.                                                                                                         | `src/components/forms/CategoryQuestionFields.tsx`                                                                                                                                                                                                                                                                                                                                          | ⚠️ File is named `CategoryQuestionFields.tsx` but exports `CoverageQuestions`. These should be aligned. ⚠️ Default section-to-category mapping is hardcoded — confirm if this needs to move into client config for all clients or only for clients that define `coverageQuestions`.                |

---

## 2. Naming Cleanup Required

The following source file/export mismatches were identified during this inventory. These should be resolved before Storybook stories are written:

| Current file                 | Current export         | Should be                                                                                |
| ---------------------------- | ---------------------- | ---------------------------------------------------------------------------------------- |
| `ConditionalGroup.tsx`       | `SubQuestionContainer` | Align: rename export to `ConditionalGroup`, or rename file to `SubQuestionContainer.tsx` |
| `DynamicList.tsx`            | `AddList`              | Rename export to `DynamicList`                                                           |
| `DynamicListItem.tsx`        | `AddListItem`          | Rename export to `DynamicListItem`                                                       |
| `CategoryQuestionFields.tsx` | `CoverageQuestions`    | Align: rename file to `CoverageQuestions.tsx`                                            |

---

## 3. Open Questions

| #   | Area                                        | Question                                                                                                                                                                                                        |
| --- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `FieldRenderer` label mode                  | Confirm whether `labelVariant: 'standard'` on `FieldDefinition` is the production mechanism replacing the `?labelStandard` URL flag, and when the URL flag will be removed.                                     |
| 2   | `FieldRenderer` → `percent`                 | `percent` format exists in `FieldRenderer` (detected via `field.format`) but is not in `FieldInputType`. Should it be added to the type union?                                                                  |
| 3   | `FieldRenderer` → phone type                | Phone type options (Mobile / Home / Business) are hardcoded. Confirm if any client needs to override them.                                                                                                      |
| 4   | `FieldRenderer` → currency                  | `CURRENCY_FIELD_IDS` hardcoded set is a legacy mechanism. Confirm if all currency fields now declare `format: 'currency'` so the set can be removed.                                                            |
| 5   | `FieldRenderer` → radio                     | `ToggleButtonGroup` uses `aria-pressed` semantics. For a radio-style group, `role="radiogroup"` + `role="radio"` + `aria-checked` is more semantically correct. Confirm whether this is a known gap to address. |
| 6   | `FieldRenderer` → SSN                       | Masked SSN input: confirm screen reader behavior. Bullet characters may be announced literally. Consider `aria-label` override on the input.                                                                    |
| 7   | `SelectableOptionRow`                       | Should it accept a `selected` prop for its own visual selected state, or is selected styling always inherited from the child input?                                                                             |
| 8   | `SubQuestionContainer` / `ConditionalGroup` | Confirm: should this component eventually absorb show/hide logic (becoming the `ConditionalFieldGroup` described in Tier 1), or remain a pure visual wrapper with the parent controlling visibility?            |
| 9   | `DynamicList`                               | Confirm min/max item count requirements (e.g. at least 1 beneficiary required). Confirm whether item removal needs a confirmation `DialogModal`.                                                                |
| 10  | `DynamicListItem`                           | Remove button accessible name: should it include the item's content to distinguish "Remove Beneficiary: John Smith" from a generic "Remove"?                                                                    |
| 11  | `ApplicantSection`                          | Confirm the full typed set of valid `ApplicantSectionId` values (e.g. `'self' \| 'spouse' \| 'child-1' \| 'child-2'`).                                                                                          |
| 12  | `CoverageQuestions`                         | Confirm whether the default section-to-category mapping (`defaultPersonalSections`, etc.) should move into client config or remain as a fallback default.                                                       |

---

## 4. Next Tiers

| Tier               | Categories                                                   | Prerequisite                                                |
| ------------------ | ------------------------------------------------------------ | ----------------------------------------------------------- |
| **Tier 3**         | `navigation/` — Stepper detail, AppHeader actions, AppFooter | LAY-001 `AppLayout` variant names confirmed                 |
| **Tier 4**         | `feedback/` — AlertBanner, LoadingSpinner, empty states      | None — can proceed                                          |
| **Tier 5**         | `overlays/` — DrawerPanel, DialogModal, QuoteEstimator       | `QuoteModal`/`QuoteEstimator` merge must be completed first |
| **Tier 6**         | `content/` — help panels, disclosures, notes, product info   | None — can proceed in parallel                              |
| **Copy inventory** | All user-facing text by page                                 | Can begin at any point                                      |
