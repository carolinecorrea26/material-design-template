# Site Components Inventory — Tiers 3–6: Navigation, Feedback, Overlays, Content

> Extends: _Site Components Inventory — Tier 2: Forms_
> Scope: `src/components/navigation/`, `src/components/feedback/`, `src/components/ui/` (overlay shells), `src/components/content/`, `src/content/`
> Status: Draft — items marked ⚠️ are open questions

---

## Tier 3 — Navigation

Component IDs: `NAV-###`

| Component ID | Component / Pattern                                          | Category              | Purpose                                                                                                                                                                                                                                                           | Pages / Locations                                                                                                                           | Behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Variants / States                                                                                                                                                       | Content / Data Inputs                                                                                                                 | Rules / Conditions                                                                                                                                 | Validation / Errors | Client Configurability                                                                                                                     | Accessibility                                                                                                                                                                                                                                                    | Source Reference                            | Status / Open Questions                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------ | ------------------------------------------------------------ | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NAV-001      | `AppHeader` (file: `Header.tsx`)                             | Navigation            | Persistent top bar: client logo, optional hamburger menu with application tools, optional cart summary icon, and an overall progress bar. No Save & Exit action exists in the current source — the menu provides "Continue Saved Application" navigation instead. | All pages via `AppLayout`                                                                                                                   | Sticky `AppBar` (MUI) that slides away on scroll via `useScrollTrigger`. Logo falls back to text (`client.branding.name`) on image error. When `variant === 'applicationForm'` and the current page is a form page (not home/receipt), a `LinearProgress` bar renders below the toolbar showing `getFormProgressPercent()`. Non-`applicationForm` variants suppress all action icons.                                                                                                                                                          | `applicationForm` — logo + cart icon + menu icon + progress bar; `homepage` / `advisorLogin` / `advisorSend` / `resumeEmailCode` — logo only, no icons, no progress bar | `client: ClientConfig`; `variant?: AppLayoutVariant` (default `'applicationForm'`)                                                    | Progress bar and action icons shown only in `applicationForm` variant and only when current page is a form page and not `receipt`                  | None                | Logo asset, `logoAlt`, `branding.name` from `ClientConfig`; all other content (support info, tool labels) from `ClientConfig`              | `role="banner"` via MUI `AppBar`; logo has `alt` from `client.branding.logoAlt`; `MenuIcon` button labeled `"Open application navigation menu"`                                                                                                                  | `src/components/layout/Header.tsx`          | ⚠️ No "Save & Exit" action exists in current source — Tier 1 inventory assumed it would be here. Confirm whether this feature is planned or removed. ⚠️ The `homepage` variant currently receives `variant="applicationForm"` from its route; confirm whether the homepage actually uses a distinct variant.                                                                                                                                                                                  |
| NAV-002      | `AppMenuDrawer` (inline in `Header.tsx`)                     | Navigation / Overlay  | Right-anchored drawer opened by the hamburger menu icon. Contains three sections: "Continue Saved Application," "Application Tools" (four tool buttons), and "Contact Us."                                                                                        | All `applicationForm` pages, launched from `AppHeader`                                                                                      | Opens as a right-side MUI `Drawer`. "Continue Saved Application" navigates to `/resume`. "Application Tools" launches secondary drawers: About Coverage → `AppDrawer` + `CoverageOptionsDrawerContent`; Get Quote → `AppDrawer` + `QuoteEstimator`; Needs Calculator → `AppDrawer` + `CoverageNeedsCalculator`; FAQ → placeholder (no action currently). Contact Us section renders support contact details from `ClientConfig`.                                                                                                               | Open; closed                                                                                                                                                            | `client: ClientConfig` (support section); tool buttons internally trigger overlay state                                               | Always visible to `applicationForm` variant users                                                                                                  | None                | Support contact details from `ClientConfig.support`                                                                                        | Drawer has close button labeled `"Close application navigation menu"`; all tool buttons are labeled; contact links use descriptive text                                                                                                                          | `src/components/layout/Header.tsx` (inline) | ⚠️ FAQ button currently has no action — confirm intended behavior. ⚠️ Component is inline in `Header.tsx`; consider extracting to `AppMenuDrawer.tsx` to reduce file size (Header.tsx is ~24KB).                                                                                                                                                                                                                                                                                              |
| NAV-003      | `FormVerticalStepper` (default export of `Stepper.tsx`)      | Navigation            | Vertical left-rail stepper (desktop) / collapsed inline stepper (mobile) showing application progress steps. Completed steps are clickable and navigate back.                                                                                                     | All `applicationForm` pages — rendered inside `RoutePage` wrapping the page content                                                         | Computes `activeSteps` (filtered to remove skipped pages via `getActiveProgressSteps`) and the active step index. Desktop: sticky left-rail `Stepper` + right content column. Mobile: `Stepper` with `StepContent` wrapping the page content, connector and borders suppressed. Completed steps have `cursor: pointer` and navigate to the first page in that step's `pageIds`. Sub-steps within the active step are surfaced via `VerticalStepperBreadcrumbs` (see NAV-004).                                                                  | Desktop (two-column grid, stepper sticky); mobile (single-column, stepper + inline content)                                                                             | `pageId: PageId`; `children: ReactNode`                                                                                               | Only rendered in `applicationForm` variant. Steps are derived live from `getActiveProgressSteps(values)` — skipped pages remove steps dynamically. | None                | Step labels from `getContent().navigation.progressStepLabels` — client-configurable via content config                                     | MUI `Stepper` step labels; completed steps rendered as `<button>` equivalents (click handler on `StepLabel`); active/pending steps are non-interactive; ⚠️ no explicit `role="list"` override — relies on MUI's default Stepper ARIA                             | `src/components/navigation/Stepper.tsx`     | ⚠️ Tier 1 described the stepper as "read-only / no direct navigation" but completed steps are in fact clickable and navigate back. Inventory should be corrected. ⚠️ Confirm desired ARIA pattern: MUI Stepper does not use `role="list"` — verify whether current markup is acceptable or if a custom pattern is needed.                                                                                                                                                                     |
| NAV-004      | `VerticalStepperBreadcrumbs` (named export of `Stepper.tsx`) | Navigation            | Horizontal breadcrumb trail showing sub-pages within the current stepper step. Health pages are consolidated into a single "Health" breadcrumb. Completed sub-pages are clickable.                                                                                | Rendered inside `RoutePage` for steps that have multiple sub-pages (e.g., the Review step containing Review, Health sub-pages, and Payment) | Reads `activeSteps` and derives breadcrumb entries for the current step's `pageIds`. Health pages (`health-si`, `health-qd`, `health-di`, `health-cir`) are consolidated into one "Health" entry navigating to the first active health page. Returns `null` if there is only one sub-page entry. Completed entries are `<button>` elements; current/pending are `<span>` elements. A custom DOM event (`form:pendingbreadcrumbcompletion`) can mark an entry as visually completed before navigation completes (used by page submit handlers). | Default (multiple sub-pages visible); null (single sub-page, renders nothing)                                                                                           | `pageId: PageId`                                                                                                                      | Rendered only when current step has more than one breadcrumb entry. Pending completion state is cleared on page change.                            | None                | Sub-page labels from `getPageNavTitle(pid)` — sourced from `pages` config                                                                  | Completed entries render as `<button>` (keyboard accessible); pending entries render as `<span>`; ⚠️ no `aria-current="page"` on the active breadcrumb entry; ⚠️ MUI `Breadcrumbs` may not announce navigation context to screen readers without additional ARIA | `src/components/navigation/Stepper.tsx`     | ⚠️ No `aria-current="page"` on the current breadcrumb entry — add for screen reader clarity. ⚠️ `form:pendingbreadcrumbcompletion` custom event is an internal contract between `RoutePage` and this component; document or formalize.                                                                                                                                                                                                                                                        |
| NAV-005      | `AppFooter` (file: `Footer.tsx`)                             | Navigation            | Persistent bottom bar with three columns: administered-by section (client contact info, license info), underwritten-by section (insurer name, policy form, address, ratings, legal links), and legal disclosures block.                                           | All pages via `AppLayout`                                                                                                                   | Static display. Responsive grid: 1 column on mobile, 2 columns on `md`, 3 columns on `lg`. Links open in new tab. "Terms of Use" and "Privacy Notice" links trigger local state (`showTermsOfUse`, `showPrivacyNotice`) — both currently render `aria-hidden` placeholder `<Box>` elements (modals not yet implemented). Financial ratings logo is hardcoded to `/logo.svg` with `alt="New York Life Logo"`.                                                                                                                                   | Default                                                                                                                                                                 | `client: ClientConfig` (administered-by section); `footerContent` from `getContent().footer` (underwritten-by section and legal text) | Always rendered on all layout variants                                                                                                             | None                | Most text content sourced from `getContent().footer` (client-configurable via content config); contact details from `ClientConfig.support` | `<footer>` element used directly; links use `rel="noopener noreferrer"`; ⚠️ no `role="contentinfo"` explicitly set (relies on `<footer>` implicit role — valid but confirm intent)                                                                               | `src/components/layout/Footer.tsx`          | ⚠️ Terms of Use and Privacy Notice modals are not implemented — both state flags are wired but the content renders as `aria-hidden` placeholders. These need design and implementation. ⚠️ Ratings logo (`/logo.svg`, alt `"New York Life Logo"`) is hardcoded — needs to come from content config for multi-client use. ⚠️ Confirm whether the footer is intentionally shown on all layout variants including `resumeEmailCode` and advisor pages, or should have variant-aware suppression. |
| NAV-006      | `CookieBanner`                                               | Navigation / Feedback | Fixed cookie-consent banner shown until the user dismisses it, at which point consent is recorded in `localStorage`.                                                                                                                                              | All pages via `AppLayout` (when consent not yet accepted)                                                                                   | Fixed-position `Alert` (bottom-right on desktop, full-width bottom on mobile). Persists across page navigation. Dismiss writes `cookieConsent: "accepted"` to `localStorage`. Content (notice text + "Learn more" link) is hardcoded.                                                                                                                                                                                                                                                                                                          | Visible; dismissed (unmounts)                                                                                                                                           | `onClose: () => void`                                                                                                                 | Shown on first visit (no `localStorage` key). Hidden after dismiss.                                                                                | None                | Notice text and "Learn more" link are hardcoded — ⚠️ should be client-configurable or sourced from content config                          | ⚠️ No `role` or `aria-live` on the banner; dismiss button (`CloseIcon`) has no accessible label                                                                                                                                                                  | `src/components/layout/CookieBanner.tsx`    | ⚠️ Cookie banner text and "Learn more" link are hardcoded. Needs to be content-config-driven. ⚠️ Dismiss `IconButton` has no `aria-label`. ⚠️ Banner has no `role="alert"` or announcement on appearance.                                                                                                                                                                                                                                                                                     |

---

## Tier 4 — Feedback

Component IDs: `FBK-###`

| Component ID | Component / Pattern      | Category | Purpose                                                                                                                                                           | Pages / Locations                                                               | Behavior                                                                                                                                                                              | Variants / States             | Content / Data Inputs                  | Rules / Conditions                                  | Validation / Errors   | Client Configurability                                                            | Accessibility                                                                                                                                                | Source Reference                                     | Status / Open Questions                                                                                                                                                                                                                                                                                                                                                               |
| ------------ | ------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | -------------------------------------- | --------------------------------------------------- | --------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FBK-001      | `PageErrorAlert`         | Feedback | Renders a full-width error `Alert` above the form area when a page-level error string is present. Returns null when no error.                                     | Any page that produces a page-level error (e.g., async failures in `RoutePage`) | MUI `Alert` with `severity="error"`, wrapped in a `Box` with `mb: 2`. No dismiss control — persists until the parent clears the error string.                                         | Error (visible); empty (null) | `error?: string`                       | Renders only when `error` is truthy                 | None — purely display | Error message text is passed from parent                                          | MUI Alert renders with `role="alert"` implicitly; ⚠️ no explicit `role="alert"` set — confirm MUI Alert's default behavior provides an assertive live region | `src/components/feedback/PageErrorAlert.tsx`         | This component corresponds to `UI-004 AlertBanner` from Tier 1, but is a simpler, error-only implementation. It does not cover info/warning/success severities or placement variants described in Tier 1. ⚠️ Confirm whether a full `AlertBanner` (with severity, placement, dismissible, autoDismiss props) is still planned, or whether `PageErrorAlert` is the production pattern. |
| FBK-002      | `ProgressSavedSnackbar`  | Feedback | Transient success snackbar shown when autosave completes successfully. Auto-dismisses after 2 seconds.                                                            | `RoutePage` (triggered on autosave success)                                     | MUI `Snackbar` + `Alert variant="filled" severity="success"`. Anchored top-center. Auto-dismisses via `autoHideDuration={2000}`. Dismiss control provided by `Alert`'s `onClose`.     | Open; closed                  | `open: boolean`; `onClose: () => void` | Controlled by parent; shown transiently on save     | None                  | "Progress saved" message is hardcoded — ⚠️ should be configurable                 | MUI Snackbar with `Alert` — `role="status"` via MUI's filled Alert; polite live region behavior                                                              | `src/components/feedback/ProgressSavedSnackbar.tsx`  | ⚠️ "Progress saved" text is hardcoded — should come from content config.                                                                                                                                                                                                                                                                                                              |
| FBK-003      | `PageTransitionSkeleton` | Feedback | Loading skeleton shown during page-to-page transitions while the next page's data resolves. Renders three rounded skeleton blocks and an optional status message. | `RoutePage` — shown during page transitions                                     | Three `Skeleton variant="rounded"` blocks with fixed heights. Optional `message` renders above using `variant="formTransitionStatus"` typography. No spinner — pure skeleton pattern. | With message; without message | `message?: string`                     | Rendered while `RoutePage` is loading the next page | None                  | Status message text is passed from parent (driven by `transitionMessages` config) | No interactive role; decorative loading indicator; ⚠️ no `role="status"` or `aria-busy` — screen readers receive no announcement of loading state            | `src/components/feedback/PageTransitionSkeleton.tsx` | ⚠️ No accessible loading announcement (`role="status"` or `aria-busy`). ⚠️ No `LoadingSpinner` component exists in source — the `UI-005 LoadingSpinner` described in Tier 1 is not implemented. Confirm whether `PageTransitionSkeleton` covers all loading states or whether a spinner overlay is still needed (e.g., for E-Sign submission, payment processing).                    |

**Note:** The `UI-005 LoadingSpinner` documented in Tier 1 does not exist in the current source. Page transitions use `PageTransitionSkeleton`. No overlay/full-page loading component was found. This is an implementation gap — confirm whether a spinner overlay is needed for submission flows.

---

## Tier 5 — Overlays

Component IDs: `OVL-###`

| Component ID | Component / Pattern | Category | Purpose                                                                                                                                                                                                 | Pages / Locations                                                                                                                                   | Behavior                                                                                                                                                                                                                                                                                                                                       | Variants / States                                                                             | Content / Data Inputs                                                                                                                                           | Rules / Conditions                                                                      | Validation / Errors                                                                                            | Client Configurability                                       | Accessibility                                                                                                                                                                                                                                                                                              | Source Reference                    | Status / Open Questions                                                                                                                                                                                                                                                                                                                                                                           |
| ------------ | ------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OVL-001      | `AppDrawer`         | Overlays | Generic slide-in panel shell for secondary content (help text, tools, coverage info). Right-anchored on desktop, bottom-anchored on mobile. Optional swipeable mode for user-initiated drawers.         | Coverage page help, Quote Estimator, Needs Calculator, Coverage Summary, About Coverage — all triggered from `AppMenuDrawer` or inline page actions | Responsive anchor: `right` on `md`+, `bottom` on `sm`-. Optional `title` prop renders a sticky header with title text and a close `IconButton`. Content area is scrollable. On mobile, if `swipeable=true`, renders `SwipeableDrawer` with `disableSwipeToOpen` (open only via button trigger). Always closes on backdrop click.               | With title header; without title header (content manages own header); swipeable (mobile only) | `open: boolean`; `onClose: () => void`; `title?: ReactNode`; `swipeable?: boolean`; `children: ReactNode`                                                       | Anchor driven by breakpoint automatically                                               | None                                                                                                           | Title text is caller-supplied                                | MUI `Drawer` defaults: focus is trapped when open; Escape closes. Close button labeled `"Close drawer"`. ⚠️ When no `title` is provided, there is no `aria-labelledby` on the drawer — caller must ensure the content provides a visible heading for screen readers.                                       | `src/components/ui/AppDrawer.tsx`   | This is the `DrawerPanel` described in Tier 1 (UI-010). Confirmed name is `AppDrawer`. ⚠️ When `title` is omitted (e.g., `CoverageSummary` manages its own header), confirm that the drawer content provides a heading that can serve as the dialog label.                                                                                                                                        |
| OVL-002      | `AppModal`          | Overlays | Generic blocking modal dialog shell for content that needs full-screen focus. Full-screen on mobile, constrained dialog on desktop. No confirm/cancel button pattern — content manages its own actions. | Coverage details popup (inline in `Header.tsx`); any future blocking modal needs                                                                    | MUI `Dialog`. Full-screen on mobile (`fullScreen={!isDesktop}`). Desktop: `maxHeight: 85vh`, configurable `maxWidth` (default 900px). `DialogTitle` with title text and close `IconButton`. `DialogContent` with dividers.                                                                                                                     | Standard (desktop constrained); full-screen (mobile)                                          | `open: boolean`; `onClose: () => void`; `title: ReactNode`; `children: ReactNode`; `maxWidth?: number`                                                          | Backdrop click and close icon both dismiss                                              | None                                                                                                           | Title is caller-supplied                                     | MUI `Dialog` handles focus trapping and `aria-labelledby` via `DialogTitle`. Close button labeled `"Close"`. ⚠️ `role="dialog"` vs `role="alertdialog"` is not differentiated — all use `role="dialog"` from MUI default. For destructive confirmations, `alertdialog` would be more semantically correct. | `src/components/ui/AppModal.tsx`    | This is the `DialogModal` described in Tier 1 (UI-011). Confirmed name in source is `AppModal`. The two-button confirm/cancel pattern from Tier 1 is not implemented — `AppModal` is content-only. ⚠️ Confirm whether a separate confirm/cancel dialog component is needed (e.g., for DynamicList item removal), or whether callers compose confirm/cancel buttons inside `AppModal`.             |
| OVL-003      | `QuoteEstimator`    | Overlays | Standalone interactive quote tool. User selects coverage categories, enters demographic inputs, and sees estimated premiums per product. Selections do not apply to the main form.                      | Rendered inside `AppDrawer` via `AppMenuDrawer` ("Get Quote" button). Also referenced from `HomeQuoteCard` on the homepage.                         | Multi-step interactive form: (1) category chip selection, (2) demographic inputs (age/DOB, gender, smoker, income/expenses/hours depending on category requirements), (3) "Show Products" → estimated premiums per product. `CircularProgress` during rate calculation. No Apply action to the main form in the current drawer implementation. | Input (awaiting user); calculating; result; error (quote unavailable)                         | Coverage categories from `getActiveClientCoverages()`; demographic field requirements from `getCategoryRequirements()`; premium from `estimateMonthlyPremium()` | Shown from menu on any application page; stateless relative to `ApplicationFormContext` | Input validation for required demographic fields (shown on "Show Products" attempt via `fieldsAttempted` flag) | Coverage products and premium logic are client-config-driven | Inputs are labeled via `FormLabel` / `InputLabel`; ⚠️ results region lacks `aria-live` — estimated premiums appearing after calculation are not announced to screen readers                                                                                                                                | `src/components/QuoteEstimator.tsx` | ⚠️ `QuoteModal.tsx` (39KB) also exists and is a substantially different, fuller implementation with an "Apply to form" action, `RateFrequencyToggle`, and `SelectionGroup`. The `QuoteModal`/`QuoteEstimator` merge from Tier 1 is **still unresolved** — two separate components exist. Confirm which becomes the canonical version and where it renders. ⚠️ No `aria-live` on the results area. |

---

## Tier 6 — Content

Component IDs: `CNT-###`

| Component ID | Component / Pattern                                                  | Category | Purpose                                                                                                                                                                                                           | Pages / Locations                                                                                                   | Behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Variants / States                     | Content / Data Inputs                                                                                                                         | Rules / Conditions                                                                 | Validation / Errors | Client Configurability                                                                                                                                                             | Accessibility                                                                                                                                                | Source Reference                                    | Status / Open Questions                                                                                                                                                                                                                                                                                   |
| ------------ | -------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CNT-001      | `FormHelpChips` (exported as `FormHelpChips`; file: `HelpChips.tsx`) | Content  | Horizontally scrollable row of `Chip` buttons that trigger contextual help drawers for specific topics on a page. On mobile, shows a fade gradient when chips overflow.                                           | Application form pages that define help items (Coverage, Health, Profile, Review, and others via page-level config) | Renders MUI `Chip` components in a horizontally scrollable `Stack`. `ResizeObserver` and `window.resize` detect overflow to show/hide the right-edge fade on mobile. Chips call `onSelect(id)` on click — the caller is responsible for opening the appropriate drawer or modal with the corresponding help content. Returns null if no items.                                                                                                                                                                       | Default (chips present); empty (null) | `items: HelpChipItem[]` (`{ id: string; label: string }`); `onSelect: (id: string) => void`; `maxWidth?: number \| string` (default `"80ch"`) | Rendered in `PageHeader` area when the page config provides help items             | None                | Help item labels are sourced from page-level config (client-configurable)                                                                                                          | Chips are `clickable` MUI Chips with `HelpOutlineRoundedIcon`; keyboard-accessible; ⚠️ chips have no `aria-haspopup` to signal that they open a panel        | `src/components/content/HelpChips.tsx`              | The associated type `FormPageHelpItem` (extends `HelpChipItem` with `title` and `content`) is also exported from this file — this is the complete help item definition used by page configs and `helpContent.tsx`.                                                                                        |
| CNT-002      | `QuickDecisionDrawerContent`                                         | Content  | Static informational content explaining the QuickDecision℠ underwriting process. Rendered inside an `AppDrawer`.                                                                                                  | Review page (and potentially Health pages) — triggered by a help chip or inline link                                | Pure content component. Two sections ("What to expect" and "Important to know") with bulleted lists. Uses `QuickDecisionMark` sub-component to render the "QuickDecision℠" trademark consistently.                                                                                                                                                                                                                                                                                                                   | Default only                          | None — static content                                                                                                                         | Shown when user triggers the QuickDecision help item                               | None                | Text is hardcoded — ⚠️ should be sourced from content config for client flexibility                                                                                                | Content is static text; no interactive elements; `QuickDecisionMark` uses `<sup>` for the SM superscript                                                     | `src/components/content/QuickDecisionExplainer.tsx` | ⚠️ Content is hardcoded JSX — should be moved to content config (`getContent().help.quickDecision`) to support client overrides. `QuickDecisionMark` is also exported from this file and used in `helpContent.tsx`.                                                                                       |
| CNT-003      | Help drawer content functions (`helpContent.tsx`)                    | Content  | Collection of content-rendering functions and React components for all help drawers across the application. Each function/component renders static or lightly dynamic informational content for a specific topic. | Invoked by page configs to populate `FormPageHelpItem.content`; rendered inside `AppDrawer` or `AppModal`           | Each export is a React component or element-producing function. Content is a mix of static text (sourced from `getContent()`) and dynamic content (e.g., `CoverageOptionsDrawerContent` uses `getActiveClientCoverages()` to render the current client's coverage product list). `ApplicationReviewDrawerContent` accepts an `onOpenQuickDecision` callback to enable inline cross-drawer navigation. `InlineDrawerLink` is a styled inline span acting as a keyboard-accessible link trigger within paragraph text. | Varies by content function            | Each component consumes `getContent()` (content config) and/or `getActiveClientCoverages()` (coverage config)                                 | Help content is shown contextually when a user triggers a help chip or header link | None                | Most text sourced from `getContent().help.*` (client-configurable). Coverage product list in `CoverageOptionsDrawerContent` is dynamically driven by `getActiveClientCoverages()`. | `InlineDrawerLink` uses `role="button"` with `tabIndex={0}` and keyboard handler for Enter/Space — correct accessible pattern for an inline interactive span | `src/content/helpContent.tsx`                       | ⚠️ `CoverageOptionsDrawerContent` is exported and used in `AppMenuDrawer` (About Coverage). Confirm all exported functions and their trigger points are documented in the copy inventory. ⚠️ `helpContent.tsx` is 17KB — confirm whether it should be split by page or topic domain as the content grows. |

---

## 2. Cross-Tier Naming and Structure Issues

| Issue                              | Detail                                                                                                                                                                           |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AppHeader` vs `Header.tsx`        | File is `Header.tsx`, export is `AppHeader`. Consistent with inventory naming — no change needed to the export. Consider renaming the file to `AppHeader.tsx` for clarity.       |
| `AppMenuDrawer`                    | The hamburger menu drawer is inline in `Header.tsx` with no named component. At ~200 lines, it warrants extraction to `AppMenuDrawer.tsx` to reduce `Header.tsx` file size.      |
| `AppDrawer` vs `DrawerPanel`       | Tier 1 named this `DrawerPanel`; source name is `AppDrawer`. Inventory corrected here.                                                                                           |
| `AppModal` vs `DialogModal`        | Tier 1 named this `DialogModal`; source name is `AppModal`. Inventory corrected here. The two-button confirm/cancel dialog pattern from Tier 1 is not yet implemented in source. |
| `FormVerticalStepper` vs `Stepper` | Export is `FormVerticalStepper` (default) + `VerticalStepperBreadcrumbs` (named). File is `Stepper.tsx`. These are tightly coupled and should remain co-located.                 |
| `LoadingSpinner` (Tier 1 UI-005)   | Not implemented in source. `PageTransitionSkeleton` covers page transitions; no overlay spinner exists. Implementation gap — see FBK-003.                                        |
| `AlertBanner` (Tier 1 UI-004)      | Not implemented as a unified component. `PageErrorAlert` covers the error case only. Implementation gap.                                                                         |

---

## 3. Open Questions

| #   | Area                                    | Question                                                                                                                                                                                                                                         |
| --- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `AppHeader` / Save & Exit               | No "Save & Exit" action exists in source. Tier 1 described it as a primary header action. Confirm whether this feature is planned, descoped, or replaced by the menu's "Continue Saved Application" link.                                        |
| 2   | `AppHeader` / homepage variant          | The homepage route appears to use `variant="applicationForm"` — confirm whether it should use `variant="homepage"` to suppress the menu icon and progress bar.                                                                                   |
| 3   | `AppMenuDrawer` FAQ                     | "FAQ" tool button in the menu has no action. Confirm intended behavior (navigate to a page, open a drawer, external link).                                                                                                                       |
| 4   | `AppFooter` / Terms & Privacy           | Terms of Use and Privacy Notice links trigger local state but render hidden placeholders. These modals are not implemented. Confirm design and content.                                                                                          |
| 5   | `AppFooter` / ratings logo              | `/logo.svg` with `alt="New York Life Logo"` is hardcoded. Needs to come from content config for multi-client use.                                                                                                                                |
| 6   | `AppFooter` / variant behavior          | Confirm whether footer should render differently (or not at all) on utility variants (`advisorLogin`, `resumeEmailCode`).                                                                                                                        |
| 7   | `CookieBanner`                          | Cookie notice text and "Learn more" link are hardcoded. Move to content config. Close `IconButton` needs `aria-label`.                                                                                                                           |
| 8   | `FormVerticalStepper` / back-navigation | Completed steps are clickable and navigate back. Tier 1 described the stepper as read-only. Confirm and correct Tier 1.                                                                                                                          |
| 9   | `VerticalStepperBreadcrumbs`            | No `aria-current="page"` on the active breadcrumb entry. Add for screen reader clarity.                                                                                                                                                          |
| 10  | `PageErrorAlert` vs `AlertBanner`       | `PageErrorAlert` covers only error severity. Confirm whether the full `AlertBanner` (Tier 1 UI-004) with severity/placement/dismissible props is still planned.                                                                                  |
| 11  | `PageTransitionSkeleton`                | No `role="status"` or `aria-busy` — screen readers receive no loading announcement. Add accessible loading state.                                                                                                                                |
| 12  | `LoadingSpinner` gap                    | No overlay spinner exists in source. Confirm whether one is needed for E-Sign submission, payment processing, and other blocking async operations.                                                                                               |
| 13  | `AppModal` / confirm-cancel pattern     | `AppModal` is content-only; no confirm/cancel button pattern exists. Confirm whether a separate destructive-action dialog (with confirm + cancel) is needed, or whether callers compose buttons inside `AppModal`.                               |
| 14  | `QuoteEstimator` / `QuoteModal` merge   | Two separate implementations exist (`QuoteEstimator.tsx` ~24KB, `QuoteModal.tsx` ~40KB). **Blocking for Storybook.** Confirm which is canonical, where it renders, and whether the "Apply to form" action from `QuoteModal` should be preserved. |
| 15  | `QuoteEstimator`                        | Results area has no `aria-live`. Estimated premiums appearing after calculation are not announced to screen readers.                                                                                                                             |
| 16  | `QuickDecisionDrawerContent`            | Content is hardcoded JSX. Move to `getContent().help.quickDecision` for client override support.                                                                                                                                                 |
| 17  | `helpContent.tsx`                       | Confirm whether the file should be split by topic domain as the application grows.                                                                                                                                                               |

---

## 4. Implementation Gaps Summary

These items appear in the Tier 1 inventory as designed components but have no corresponding source implementation:

| Gap                          | Tier 1 ID   | Detail                                                                                                                                   |
| ---------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `AlertBanner` (unified)      | UI-004      | Only `PageErrorAlert` (error-only) exists. Info, warning, success, dismissible, autoDismiss, and placement variants are not implemented. |
| `LoadingSpinner`             | UI-005      | Not implemented. Page transitions use `PageTransitionSkeleton`. No overlay spinner for submission/payment flows.                         |
| `DialogModal` confirm/cancel | UI-011      | `AppModal` is content-only. Two-button confirm/cancel pattern is not implemented.                                                        |
| Terms of Use modal           | UI-002 area | Footer triggers state flag but renders a hidden placeholder — modal is not implemented.                                                  |
| Privacy Notice modal         | UI-002 area | Same as above.                                                                                                                           |
| Session timeout dialog       | UI-011      | Not implemented. Documented as a known gap in Tier 1.                                                                                    |
| Save & Exit action           | UI-001      | Not present in `AppHeader`. May be descoped — needs confirmation.                                                                        |

---

## 5. Storybook Blockers

These items must be resolved before Storybook stories can be written:

| Blocker                             | Detail                                                                                                                                                            |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `QuoteModal`/`QuoteEstimator` merge | Two implementations, one canonical version needed (Open question 14)                                                                                              |
| Naming cleanup from Tier 2          | `AddList` → `DynamicList`, `AddListItem` → `DynamicListItem`, `CategoryQuestionFields` → `CoverageQuestions`, `ConditionalGroup`/`SubQuestionContainer` alignment |
| `AlertBanner` gap                   | Decide whether to implement the full component or document `PageErrorAlert` as the production pattern                                                             |
| `AppModal` confirm/cancel           | Decide pattern before writing stories for destructive actions                                                                                                     |

---

## 6. Next Phases

| Phase                           | Scope                                                                                                           | Prerequisite                      |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| **Storybook**                   | All components from Tiers 1–6                                                                                   | Resolve blockers above            |
| **Implementation requirements** | Page/flow requirements, conditional routing, field definitions, error/loading/empty states, client config model | Tiers 1–6 complete (done)         |
| **Copy inventory**              | All user-facing text by page                                                                                    | Can begin immediately in parallel |
